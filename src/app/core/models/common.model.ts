/** Tono semántico compartido por badges, iconos y alertas. */
export type SemanticTone = 'ok' | 'warn' | 'bad' | 'info' | 'neutral' | 'primary';

/** Degradado de avatar de marca (mapea a clases .av-* del sistema de diseño). */
export type AvatarTone = 'ink' | 'honey' | 'ok' | 'slate' | 'info' | 'neutral';

/** Tipo de soporte documental (mapea a clases .fx-* del sistema de diseño). */
export type DocKind = 'xls' | 'pdf' | 'doc' | 'img';

/** Moneda manejada por el proceso (RNF-CO-02). */
export type Currency = 'USD' | 'COP';
